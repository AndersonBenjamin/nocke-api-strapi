module.exports = {
  rest: {
    defaultLimit: 1000,  // Ajuste para um valor alto ou remova para buscar todas de uma vez
    maxLimit: 1000,      // Ajuste para um valor alto ou remova para buscar todas de uma vez
    withCount: true,
  },
};
